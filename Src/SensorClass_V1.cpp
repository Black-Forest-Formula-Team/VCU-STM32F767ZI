class SensorValues {
  private:             // Access specifier
    int throttle;        // Attribute (int variable)
  public:
    boolean setThrottlePosition(int throttleInput){
        //tbd
        throttle = throttleInput;
    }
    int getThrottlePosition(
        return throttle;
    )
};